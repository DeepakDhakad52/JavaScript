// What is DOM (Document Object Model)

// -> Firstly Browser inspect all code of HTML file and it will create a tree

// After inspection of HTML file browser will create an object, that object is known as DOCUMENT and it will be added to the WINDOW object.

// console.log(window.document);
// console.dir(window.document);
console.dir(document);




// Events : Click event, scroll event, pop-up event, submit event